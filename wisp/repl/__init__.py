"""REPL support package — picker, completion, and the split command modules.

Modules:
  picker.py    — interactive arrow-key/Tab option selector
  completion.py— readline Tab completion for slash commands
  commands/    — slash-command implementations (registry + thematic modules)
"""
